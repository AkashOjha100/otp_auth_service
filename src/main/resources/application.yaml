server:
  port: 9000
spring:
  application:
    name: authService
  datasource:
    url : jdbc:postgresql://localhost:5432/leapmoney
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    location: classpath:/db/migration
  data:
    redis:
      host: localhost
      port: 6379
  mail:
    host: smtp.gmail.com
    port: 587
    username: anand.orbiqe@gmail.com
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
twilio:
  sid: ${TWILIO_ACCOUNT_SID}
  token: ${TWILIO_AUTH_TOKEN}
  number:


